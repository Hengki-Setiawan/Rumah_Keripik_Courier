# API base URL (change for staging/production)
# Currently hardcoded in src/lib/api-client.ts — will use this when implemented
EXPO_PUBLIC_API_URL=https://rumah-keripik.vercel.app

# Expo token for push notifications (get from expo.dev)
EXPO_TOKEN=
